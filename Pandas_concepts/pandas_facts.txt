# .count()  : It is used to count the no. of rows generated by a specific querey.
# .values_count()  : It is used to count the no. of unique rows in a series or dataframe.

# To find the name start with specific letter or not (eg. G) : df[df[column].str.startswith("G")]
                                                        OR
                                                             : df[df[column].str[0]=="G"]


# .size()  : It is used to counts the number of rows in each group formed by groupby.
             Means in short way it counts the no. of elements in each group.


# .map(function_name)  : It is used to apply the function in each and every column.


# How to delete a col in pandas : del df["column_name"]    OR    df.drop("column_name", axis=1, inplace=True)     
                                 //Here in this way it            //Here in this way it need inplace=True
                                 dont need inplace=True 
